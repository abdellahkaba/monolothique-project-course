spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json
server:
  servlet:
    context-path: /api/v1/

# name: BOOKING ROOM HOTEL Backend API Pipeline

# on:
#   push:
#     branches:
#       - main
#       - frontend

# jobs:
#   compile:
#     runs-on: ubuntu-latest
#     name: Compile project
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Set executable permissions for mvnw
#         run: chmod +x bookingRoom/mvnw

#       - name: Setup JDK
#         uses: actions/setup-java@v4
#         with:
#           java-version: 17
#           distribution: 'corretto'

#       - name: Compile project
#         run: |
#           cd bookingRoom
#           ./mvnw clean compile

#   build-and-deploy:
#     name: Build Docker Image and Deploy to Minikube
#     runs-on: ubuntu-latest
#     needs: [compile]
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set executable permissions for mvnw
#         run: chmod +x bookingRoom/mvnw

#       - name: Set up Minikube Docker Environment
#         run: |
#           # Configure Docker to use Minikube
#           eval $(minikube -p minikube docker-env)

#       - name: Build Docker Image
#         run: |
#           docker build -t booking-api:latest -f docker/backend/Dockerfile bookingRoom

#       - name: Deploy to Minikube
#         run: |
#           # Create or update deployment in Minikube
#           kubectl create deployment booking-api --image=booking-api:latest --dry-run=client -o yaml > deployment.yaml
#           kubectl apply -f deployment.yaml
#           kubectl expose deployment booking-api --type=NodePort --port=8080
# 